完成这些步骤之后，我们就可以转入到Git的本地设置了。进行设置之前，我们必须知道Git配置文件的名称和存放的位置，根据Drupal Git文档上说明，Git的配置文件的名称为.gitconfig，于是我们习惯性地打开Git的安装目录，很悲剧的发现没有该文件，那么配置文件会在哪里呢？除了安装目录可以存放配置文件外，Windows习惯把配置文件放在以登录名字文件夹中，我们打开C:\users\[你windows登录用户]\,不出意外，应该是存在的，如果没，怎么办，创建一个，利用任何一个编辑器或命令行创建一个即可。然后点击开始 > 所有程序 > Git > 打开Git Bash，在Bash窗口里输入：